# TODO / 进度

> 说明：这份是给下一次接着干用的"存档"。上半部分是**事实性的当前状态**（已完成 / 待验证 / 已知问题），请尽量保持准确；下半部分"后续想法"是留给你自己按实现思路慢慢加的脚手架。
> 记录时间：2026-07-14。工程细节见 [README.md](README.md)。

---

## ✅ 已完成（模拟器 26.3 跑通）

- [x] EPUB 导入、解压（扁平化到 `Documents/Extracted/<书>/`）、书库网格展示、删除
- [x] WKWebView + CSS 多栏分页；点击左右区 / 左右滑动 / 底部进度条翻页
- [x] 单击屏幕中部呼出 / 收起控制条
- [x] 断点续读：记住上次页码，书库进度条同步（修好过"卡 0%"回归）
- [x] 字号 / 字体 / 行距 / 对齐 / 主题 / 页边距，实时应用（修好过"引号转义炸脚本"）
- [x] EPUB 内嵌图片 / 封面渲染（按章节目录**精确解析**图片引用 + 全量图片兜底解压 + `loadFileURL`）
- [x] 中文字体：**苹方** + **思源宋体（Source Han Serif SC）** Regular / SemiBold / Bold（Git LFS 管理，OFL 许可免费可商用）。设置面板字体芯片居中对齐 + 间距统一
- [x] 字体选择器 UI 修复：芯片图标 ZStack 垂直居中、描述文本固定宽度自动换行
- [x] 翻页只做纯水平位移；控制条显隐不再上下抽动（WebView 尺寸恒定 + `scrollTop` 锁 0）
- [x] 刘海 / 灵动岛避让：正文靠 `viewport-fit=cover` + `env(safe-area-inset-*)` 内衬，背景仍铺满全屏
- [x] 书库进度条随"打开→翻页→关闭"刷新（卡片 `.id()` 带上页码，绕开 `Book` 只按 id 判等导致的视图不刷新）
- [x] 给软件加一个 logo
- [x] **音量键翻页**：真机中已进行测试, 设置里开"音量键翻页"→ 进阅读页 → 按物理音量 +/−, 即可进行翻页.
- [x] 目录（TOC）面板, 支持跳转, 但部分内容与目录识别不准确
- [x] **阅读进度统计页**（TabView 第二标签页）：今日阅读时长（直线进度条+每日目标）、连续阅读天数、本年读完书数
- [x] **阅读计时追踪**：进入阅读页开始计时，退后台/退阅读页自动保存阅读时段
- [x] **长按书库卡片** → "标记为已读完 / 标记为未读"（影响本年读完统计）
- [x] **脚注/注释跳转**：点击 EPUB 内部链接（`<a href="#...">`）→ 自动跳转到目标页 → 底部弹出"返回原文"按钮（10s 自动消失）+ 支持多级栈式返回
- [x] **章节目录匹配修复**：top bar 显示的章节标题改用 href 匹配（不再用 index 直接取 chapters[index]），修复 spine vs NCX 不一致导致标题错位
- [x] **底部栏页码跳转**：输入页码直接跳转（1-based，自动校验范围）
- [x] **章节目录跳转修复**：JS computeChapterMetrics 改用 getBoundingClientRect + spineIndex → Swift 通过 href 映射到 NCX 章节（修复"点第60章跳到59章"）
- [x] **底部栏重新设计**：进度条改为图标按钮（点击展开可拖拽完整进度条）、三区布局（目录/进度/跳转 居左/中/右）
- [x] **每日目标折叠滚轮选择器**：点击数值展开/折叠双滚轮（小时+分钟），防 0h0m，居中显示
- [x] **连续阅读逻辑修正**：连续天数 = 连续达成每日目标的自然日数，底部显示最长连续记录
- [x] **行高对齐修复**：snapToLineHeight 让内容区高度对齐行高整数倍，避免半行截断



## 🔜 后续想法（自己按需补充/取舍）

<!-- 下面只是候选，删改随意 -->

- [ ] 翻页动画（可选：横向滑动/仿翻书；注意别破坏"纯水平位移"的稳定性）
- [ ] 书签 / 笔记 / 高亮
- [ ] 更稳的编码探测（目前按 utf8→utf16→latin1→cp1252 顺序试）
- [ ] 大图排版：`max-height: 85vh` 之外，是否需要独占整页的图片页
- [ ] 正确检测并且解析**脚注**, 并且支持来回跳转

## 🚫 明确不做（产品范围）

- Spotlight 全文搜索、快捷指令（Shortcuts）
