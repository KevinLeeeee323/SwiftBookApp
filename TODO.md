# TODO / 进度

> 说明：这份是给下一次接着干用的"存档"。上半部分是**事实性的当前状态**（已完成 / 待验证 / 已知问题），请尽量保持准确；下半部分"后续想法"是留给你自己按实现思路慢慢加的脚手架。
> 记录时间：2026-07-14。工程细节见 [README.md](README.md)。

---

## ✅ 已完成（模拟器 26.3 跑通）

- [x] EPUB 导入、解压（扁平化到 `Documents/Extracted/<书>/`）、书库网格展示、删除
- [x] WKWebView + CSS 多栏分页；点击左右区 / 左右滑动 / 底部进度条翻页
- [x] 单击屏幕中部呼出 / 收起控制条
- [x] 断点续读：记住上次页码，书库进度条同步（修好过"卡 0%"回归）
- [x] 字号 / 字体 / 行距 / 对齐 / 主题 / 页边距，实时应用（修好过"引号转义炸脚本"）
- [x] EPUB 内嵌图片 / 封面渲染（引用改写 + `loadFileURL(allowingReadAccessTo:)`）
- [x] 翻页只做纯水平位移；控制条显隐不再上下抽动（WebView 尺寸恒定 + `scrollTop` 锁 0）

## 🟡 待验证 / 已知问题

- [ ] **音量键翻页——真机验证**。模拟器可能只加音量不翻页（模拟器限制）。真机若仍不行，排查方向：`AVAudioSession` 是否真的在输出静音 WAV、`outputVolume` KVO 是否触发、HUD 是否被 `MPVolumeView` 抑制。见 `VolumeButtonHandler.swift` 与 README §5。
- [ ] **刘海机型首行贴边**：阅读区常驻全屏，极小 `marginV` 下正文首行可能靠近状态栏。必要时给内容顶部/底部加一层不随控制条变化的固定安全区内衬（注意：**不能**再把内衬跟 `showControls` 联动，否则重新引入抽动，见 README 坑①）。
- [ ] epub 没法加载图片
- [ ] 书外部的进度条, 没法实时显示打开-改变阅读进度-关闭书过程中的进度


## 🔜 后续想法（自己按需补充/取舍）

<!-- 下面只是候选，删改随意 -->
- [ ] 目录（TOC）跳转面板（`book.chapters` 已解析好，缺 UI + `goToPage` 到章首）
- [ ] 翻页动画（可选：横向滑动/仿翻书；注意别破坏"纯水平位移"的稳定性）
- [ ] 记住并显示"本章内进度 / 全书百分比"
- [ ] 书签 / 笔记 / 高亮
- [ ] 亮度调节接进设置面板
- [ ] 更稳的编码探测（目前按 utf8→utf16→latin1→cp1252 顺序试）
- [ ] 大图排版：`max-height: 85vh` 之外，是否需要独占整页的图片页

## 🚫 明确不做（产品范围）

- Spotlight 全文搜索、快捷指令（Shortcuts）
